import { Colors } from "lib/constants";

export interface IProps {
  size?: number;
  color?: Colors;
  thickness?: number;
}

export interface IWrapperProps {
  size: number;
  color: Colors;
  thickness?: number;
}
