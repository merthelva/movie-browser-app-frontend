import { Colors } from "lib/constants";

export interface ISpinnerProps {
  size?: number;
  color?: Colors;
  thickness?: number;
}

export interface IWrapper {
  size: number;
  color: Colors;
  thickness?: number;
}
