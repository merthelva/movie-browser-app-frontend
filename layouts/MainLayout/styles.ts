import styled from "styled-components";

export const Main = styled.main`
  position: relative;
  top: 60px;
  padding: var(--spacing-12x) var(--spacing-8x) var(--spacing-24x);
  text-align: center;
`;
