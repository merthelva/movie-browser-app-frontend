import { css } from "styled-components";

export default css`
  :root {
    /* Font Weights */
    --fw-thin: 300;
    --fw-normal: 400;
    --fw-semi: 500;
    --fw-bold: 600;
    --fw-bolder: 700;
  }
`;
